# SupportOps Dashboard (MVP)

A minimal realtime SupportOps Dashboard built with Node.js, Express, and Socket.io.
This repository contains a single-server demo that serves a static frontend and broadcasts realtime ticket events.

## Demo / Quickstart

1. Clone the repo:
   ```bash
   git clone https://github.com/<your-github-username>/supportops-dashboard.git
   cd supportops-dashboard
